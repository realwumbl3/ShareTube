{
    // JS project configuration for the mobile remote page sub-codebase.
    // This file is picked up when you open `pages/mobile_remote/` as a workspace.
    "compilerOptions": {
        // Use modern ECMAScript for type checking and IntelliSense.
        "target": "ES6",
        "module": "ES6",
        // Treat this page directory as the base for module resolution.
        "baseUrl": ".",
        // Editor-only path aliases for nicer imports while working on this page.
        "paths": {
            "@js/*": ["frontend/static/js/*"],
            "@css/*": ["frontend/static/@css/*"],
            "@templates/*": ["frontend/templates/*"],
            "/extension/app/*": ["/home/wumbl3wsl/ShareTube/backend/ShareTube-v1-02/extension/app/*"]
        },
        // Keep JS checking relaxed unless you prefer stricter rules.
        "checkJs": false
    },
    // Only index this page's own frontend code.
    "include": ["frontend/static/js/**/*.js", "frontend/templates/**/*.html"],
    // Explicitly exclude the rest of the monorepo when opened from above.
    "exclude": [
        "../../node_modules",
        "../../static",
        "../../templates",
        "../../migrations",
        "../../views",
        "../../models",
        "../../scripts"
    ]
}
