# SHARETUBE

Implement Chat

-   [ENTER] to enter chat input mode, [ENTER] with "" or [ESC] to cancel. [ENTER] to send.

User presence peak and user icon highlight overlay view on user joins/leaves. Maybe for other events too. Aggregate user actions. - Replaces audit from Splash.js

Start/Restart videos at now() - 2 seconds as a buffer for any unintended blocking.

Queue -becomes-> MiniApp <-reimplement- Queue component

-   Drag to reorder videos in the queue.
-   Queue button in pill becomes "Show ShareTube", Queue (length) when in room and queue has items else "Queue empty."

Waiting for [users]... overlay.

-   "Start now" button.

Popup iframe thumbnail click intercept. 

-   iframe frame has add to queue button.


# ZYX

-   zyx conditions, livevar/list condition and operations.
-   livevar/list proxies.
-   microtask callstack.
