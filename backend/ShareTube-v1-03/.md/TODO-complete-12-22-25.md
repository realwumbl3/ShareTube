# SHARETUBE

# Implement Chat

-   [ENTER] to enter chat input mode, [ENTER] with "" or [ESC] to cancel. [ENTER] to send.

# User presence peak and user icon highlight overlay view on user joins/leaves. Maybe for other events too. Aggregate user actions. - Replaces audit from Splash.js

# Start/Restart videos at now() - 2 seconds as a buffer for any unintended blocking.

# Queue -becomes-> MiniApp <-reimplement- Queue component

<!-- -   Drag to reorder videos in the queue. -->
-   Queue button in pill becomes "Show ShareTube", Queue (length) when in room and queue has items else "Queue empty."

# Waiting for [users]... overlay.

-   "Start now" button.

# Popup iframe thumbnail click intercept.

-   iframe frame has add to queue button.

# Follow leader mode. Follow the leader's video playback / video page navigation.

# Owner/Operator control playback exclusively.
<!-- -   Hide playback controls for non-operators. -->
    -    Playback is controlled by the operator indication.

# Sync mode optinal in playing room.
-   Stray from room's sync state.
-   Playback controls show again but don't control room playback.
-   Single click re-sync button. (perhaps to the right of firstparty play button)



# Schedule watch party. Schedule a watch party for a future date and time. Ready up system.

# Embed player like watch2gether.

# thin side queue filter panel. full height thumbnail scroll.

-   swipe between panels.
-   scrolling filter panel scrolls filter view.

<!-- # On end autoplay optional.

-   continue to next video button (with thumb and name) -->

# Pause mouse position share to other users.

-   only operator mode.
-   tiny username under cursors.
-   only moving cursors join pause mouse share.
-   clear movers and ui on resume.

-   mousehold draw.
    -   clear on resume.
    -   history thumbnail row top left. 
        -   restores paused position and paint content.



# Non premium users can only have 1 session at a time.

-   pill shows "opened in new window".
-   "close other window and reopen" button.

# ZYX

-   zyx conditions, livevar/list condition and operations.
-   livevar/list proxies.
-   microtask callstack.
-   check for parent render condition in false zyx-if. schedule a rerender if parent render condition becomes true.
